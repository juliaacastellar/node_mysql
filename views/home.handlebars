<main>
    <h2>Home - Biblioteca</h2>

    {{#each books}}
    <div class="book">
        <h3>Titulo: {{this.name}}</h3>
        <p>Quantidade de paginas: {{this.pageqty}}</p>
        <a href="/book/{{this.id}}">Ver mais</a>
    
    </div>
    {{/each}}
</main>